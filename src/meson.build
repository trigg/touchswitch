all_deps = [wayfire]

shared_module('touchswitch',
        ['touchswitch.cpp', 'touchswitch-title-overlay.cpp'],
        dependencies: all_deps,
        install: true,
        install_dir: join_paths(get_option('libdir'), 'wayfire'))
